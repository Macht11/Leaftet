# Local development may run without APP_PASSWORD, but hosted deployments should set it.
APP_PASSWORD=change-me

# Optional Supabase persistence for hosted deployments.
SUPABASE_URL=
SUPABASE_KEY=
SUPABASE_TABLE=leaflet_history
